apiVersion: v1
kind: Service
metadata:
  name: loki-nodeport
  namespace: observability
spec:
  selector:
    app: loki
  type: NodePort
  ports:
    - port: 3100
      targetPort: 3100
      nodePort: 30310
