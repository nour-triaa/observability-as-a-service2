apiVersion: v1
kind: Service
metadata:
  name: veeam-collector
  namespace: default
spec:
  type: NodePort
  selector:
    app: veeam-collector
  ports:
    - protocol: TCP
      port: 5050
      targetPort: 5050
      nodePort: 30050       # ← accessible via minikube ip:30050
